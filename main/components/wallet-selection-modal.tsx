"use client";

// Wallet selection modal stub — @aptos-labs dependencies removed
interface WalletSelectionModalProps {
  children: React.ReactNode;
}

export function WalletSelectionModal({ children }: WalletSelectionModalProps) {
  return <>{children}</>;
}
